# com.kruty1918.entity-health

Reusable entity-health primitives extracted from Moyva: `IHealth` (hp/destroyed/events contract), `HealthComponent` (MonoBehaviour impl), `IHealthRegistry`/`HealthRegistry` (entityId keyed lookup). Damage formulas, death/capture rules and network authority stay host-side.
